#pragma once

extern int list_row_index;

void correct_table(int& current, int maximum);
void correct_table(int& origin, int maximum, int per_page);
void input_mouse_table(int& origin, int maximum, int per_page, int per_row);
